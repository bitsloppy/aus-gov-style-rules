# Pipeline Usage

> **Status:** Placeholder — will be written in Phase 6.

This file will document how to use `aus-gov-style-rules` with the
[Anthropic content moderation cookbook](https://platform.claude.com/cookbook/capabilities-content-moderation-guide)
rule engine pattern.

**Planned sections:**

1. Using `schema/ruleset.json` as the rule definition input
2. Running deterministic rules (regex/pattern matching)
3. Running llm-assertion rules (model evaluation)
4. Combining results into a violation report
5. Integration examples (CI pipeline, API wrapper, local script)
